import React from "react";
import styled from "styled-components";

const BodyStyle = styled.section`
  height: 200vh;
`;

const Body = props => (
  <BodyStyle>
    <h2>Hi</h2>
  </BodyStyle>
);

export default Body;
